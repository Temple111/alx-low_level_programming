#include <stdio.h>

/**
 * main - this program checks if the last digit of anumber is >, < or zero
 * Return: 0 (sucessful)
 */
int main(void)
{
	int c;

	c = 0;
	while
		(c < 10) {
			putchar(c + '0');
			c++;
		}
	putchar('\n');
	return (0);
}
