#include <stdio.h>

/**
 * main - this program checks if the last digit of anumber is >, < or zero
 * Return: 0 (sucessful)
 */
int main(void)
{
char c;

c = 'a';
while
	(c <= 'z') {
		putchar(c);
		c++;
	}
putchar('\n');
return (0);
}
